# /etc/systemd/system/browser-relay.service.d/resource-limits.conf
# 小内存 VPS 的活命红线：数值按 2GB 内存机器给的，不可照抄，按你的机器调。
# OOMPolicy=stop 意味着超限时整个服务被停掉再由 Restart=always 拉起 —
# 宁可浏览器重启，不能拖死整台机器。
[Service]
MemoryAccounting=yes
MemoryHigh=850M
MemoryMax=1200M
TasksMax=220
OOMPolicy=stop
